{
  "version": 2,
  "buildCommand": "pnpm exec vite build && pnpm exec esbuild server/vercel-handler.ts --bundle --platform=node --format=cjs --packages=external --outfile=api/index.js",
  "installCommand": "pnpm install",
  "outputDirectory": "dist/public",
  "functions": {
    "api/index.js": {
      "maxDuration": 30,
      "memory": 1024
    }
  },
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/index" },
    { "source": "/(.*)",       "destination": "/index.html" }
  ]
}
